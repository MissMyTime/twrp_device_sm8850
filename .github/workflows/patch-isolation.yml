name: Patch isolation

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate patch sets
        run: ./scripts/check-patch-isolation.sh
